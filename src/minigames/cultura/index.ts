import { CulturaScene } from './CulturaScene';
import type { MinigameSceneModule } from '../types';

const cultura: MinigameSceneModule = {
  sceneKey: 'cultura',
  scene: CulturaScene
};

export default cultura;
