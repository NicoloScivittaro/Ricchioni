import { SoccerScene } from './SoccerScene';
import type { MinigameSceneModule } from '../types';

const soccer: MinigameSceneModule = {
  sceneKey: 'soccer',
  scene: SoccerScene
};

export default soccer;
