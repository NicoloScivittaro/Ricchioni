import { HotWheelsScene } from './HotWheelsScene';
import type { MinigameSceneModule } from '../types';

const hotwheels: MinigameSceneModule = {
  sceneKey: 'hotwheels',
  scene: HotWheelsScene
};

export default hotwheels;
