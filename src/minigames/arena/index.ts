import { ArenaScene } from './ArenaScene';
import type { MinigameSceneModule } from '../types';

const arena: MinigameSceneModule = {
  sceneKey: 'arena',
  scene: ArenaScene
};

export default arena;
