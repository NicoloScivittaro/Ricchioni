import { ReactionScene } from './ReactionScene';
import type { MinigameSceneModule } from '../types';

const reaction: MinigameSceneModule = {
  sceneKey: 'reaction',
  scene: ReactionScene
};

export default reaction;
