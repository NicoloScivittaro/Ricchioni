import { MemoryScene } from './MemoryScene';
import type { MinigameSceneModule } from '../types';

const memory: MinigameSceneModule = {
  sceneKey: 'memory',
  scene: MemoryScene
};

export default memory;
