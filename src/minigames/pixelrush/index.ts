import { PixelRushScene } from './PixelRushScene';
import type { MinigameSceneModule } from '../types';

const pixelrush: MinigameSceneModule = {
  sceneKey: 'pixelrush',
  scene: PixelRushScene
};

export default pixelrush;
