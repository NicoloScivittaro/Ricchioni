{
  "name": "ricchioni-party",
  "version": "0.1.0",
  "private": true,
  "description": "Party game a minigiochi SENZA tabellone — Anzio/Nettuno (Vite + TypeScript + Phaser)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.1.0"
  }
}
